# services/backend/.env.example
# Example backend environment variables (do NOT commit actual secrets)
# Copy this to .env in development and fill values.

# Application
ENV=development
PORT=8000

# File storage paths (absolute or relative)
# If left empty, defaults in code will be used (services/backend/uploads, services/backend/processed)
UPLOAD_DIR=services/backend/uploads
PROCESSED_DIR=services/backend/processed

# CORS origins (comma-separated). For dev you can use '*' but in prod set the explicit origins.
CORS_ORIGINS=http://localhost:3000

# Database (when you add DB later)
DATABASE_URL=postgresql://user:password@localhost:5432/changeanyfile

# Secrets / keys (add as GitHub secrets in CI — never commit to repo)
# EXAMPLE:
# S3_ACCESS_KEY=YOUR_S3_KEY
# S3_SECRET_KEY=YOUR_S3_SECRET
