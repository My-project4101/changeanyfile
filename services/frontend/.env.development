# services/frontend/.env.development
# Development environment variables for Vite
# Used when running `npm run dev`

# Base URL where frontend will call backend API in dev
VITE_API_BASE_URL=http://localhost:8000

# Optional: toggle verbose logging in dev (custom flag)
VITE_VERBOSE=true
