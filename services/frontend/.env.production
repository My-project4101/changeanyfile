# services/frontend/.env.production
# Production environment variables for Vite
# Used when building (npm run build) for production

# Replace with your production API endpoint
VITE_API_BASE_URL=https://api.changeanyfile.ai

# Turn off verbose logging in production
VITE_VERBOSE=false
